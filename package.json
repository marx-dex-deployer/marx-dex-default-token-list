{
  "name": "@marx-dex/marx-dex-default-token-list",
  "version": "1.3.6",
  "description": "◦ The MarX DEX default token list",
  "main": "build/marx-dex-default.tokenlist.json",
  "scripts": {
    "test": "mocha",
    "build": "rimraf build && mkdir -p build && node src/write.js > build/marx-dex-default.tokenlist.json",
    "prepublishOnly": "npm test && npm run build"
  },
  "files": [
    "build/marx-dex-default.tokenlist.json"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/marx-dex-deployer/marx-dex-default-token-list.git"
  },
  "keywords": [
    "marx-dex",
    "default",
    "evmos",
    "token",
    "list"
  ],
  "license": "GPL-3.0-or-later",
  "bugs": {
    "url": "https://github.com/marx-dex-deployer/marx-dex-default-token-list/issues"
  },
  "homepage": "https://github.com/marx-dex-deployer/marx-dex-default-token-list#readme",
  "devDependencies": {
    "@ethersproject/address": "^5.0.2",
    "@marx-dex/marx-dex-token-lists": "^1.0.3",
    "ajv": "^6.12.3",
    "chai": "^4.2.0",
    "mocha": "^8.0.1",
    "rimraf": "^3.0.2"
  }
}
